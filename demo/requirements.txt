# Demo Dependencies - NO Docker, NO Kafka required
# Just: pip install -r demo/requirements.txt
#
# For Streamlit Cloud deployment, use root requirements.txt (no heavy Spark deps)

# Core processing — only needed for local pipeline runs
pyspark==3.5.0
delta-spark==3.1.0

# Live data collection
requests>=2.31.0
sseclient-py>=1.8.0          # Server-Sent Events client (Wikimedia streams)

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.1.0
numpy>=1.24.0                # Dashboard synthetic data generator
pyarrow>=14.0.0              # Parquet file reading

# Utilities
tabulate>=0.9.0
